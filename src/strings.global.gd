## Здесь лежат глобальные строки, которые нужны для кода, а потому не попадают в ресурсы для локализации. Если строки нигде больше не используются, то можно не добавлять. Поскольку очевидно что этот класс содержит константы, используется PascalCase
extends Node

# Названия для методов и функций global.gd > gal

const Funcs: StringName = "Functions"
const Props: StringName = "Properties"
